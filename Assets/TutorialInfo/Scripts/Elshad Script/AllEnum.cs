using UnityEngine;

//This is all of the enumerator that i use for the game

//this enum is for changing the outfit order
public enum _changeOrder
{
    next = 0, prev = 1
}

//this enum is for categorize all of the outfit into these categories
public enum _skinCategories
{
    head = 0, top = 1, bottom = 2, shoes = 3, head_accesories = 4, top_accesories = 5, preset = 6
}

//this enum is for assigning gender for the NPC
public enum _CharID
{
    male = 0, female = 1
}